<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Production always uses HTTPS (Fly.io enforces it).
         Cleartext allowed so local HTTP backend works for dev/demo. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
